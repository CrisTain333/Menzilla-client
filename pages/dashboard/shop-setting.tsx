import Settings from '@/libs/modules/settings/settings';
import React from 'react';

const index = () => {
    return (
        <div>
            <Settings />
        </div>
    );
};

export default index;
